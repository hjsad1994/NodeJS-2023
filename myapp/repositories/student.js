const getAllStudents = async ({
    page,
    size,
    searchString,

}) => {
    console.log('get all student with paping')
}
const insertStudent = async ({
    name, email, languages, gender, phoneNumber, address
}) => {
    const exitstingUser = await 
}
export default {
    getAllStudents,
    insertStudent
} 